0: show the style sheet, go through the html skeleton describing each part

1: 
- draw a ball (in drawBall function)
ctx.beginPath();
ctx.arc(50, 50, 10, 0, Math.PI*2);
ctx.fillStyle = "#0095DD";
ctx.fill();
ctx.closePath();